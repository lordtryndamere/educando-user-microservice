export enum Status {
  ACTIVE = 1,
  INACTIVE = 0,
  DESACTIVATED = 4,
}
export enum Rolles {
  ADMIN = 1, // admin  -  can do everything     -  can do everything
  TEACHER = 2, // school -  can do everything     -  can do everything
  STUDENT = 3, // student - can do everything     -  can do everything
}
